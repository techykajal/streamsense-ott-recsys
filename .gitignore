# Python
.venv/
venv/
__pycache__/
*.pyc
.python-version

# Regeneratable data and scratch working dirs (NOT committed)
data/
artifacts/
tfx/
model_store/
*.mar
*.db
*.log
ott_pipeline.yaml

# Registered models live in models/ and ARE committed on purpose:
# the training->serving handoff, a POC stand-in for a model registry
# (MLflow / Vertex Model Registry / S3). So *.pt / *.onnx are NOT globally ignored.

# OS / editor
.DS_Store
.vscode/
.idea/
